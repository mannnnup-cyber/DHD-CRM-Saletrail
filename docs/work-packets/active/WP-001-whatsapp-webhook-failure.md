--- WP-001 STATUS UPDATE (Lead record, 2026-09-11) ---
Status: Review (NOT Done — blocked on live verification, per QA verdict NEEDS-EVIDENCE; Security verdict PASS review / ESCALATE pre-existing webhook gap)
Root Cause Classification: Layer B (Webhook Registration — volatile after restart) + Layer C (Callback Delivery — no persistence verification). Evidence: static/code-analysis only (api/whatsapp.ts lines 164-555, WhatsApp.tsx reconnect/gap banner, webhookInfo/setWebhook/status code paths). No live test performed: no Evolution restart/re-registration, no Vercel delivery log, no DB persistence check for a fresh inbound message, no controlled test message traced.
Evidence Limitations (explicit): live webhook config query (webhookInfo) result not captured; live Evolution container restart/re-registration not executed; live /api/whatsapp POST with test payload not confirmed; DB persistence of new inbound message not verified; syncEvolutionMessages recovery not tested.
Implementation Report (follops-integrations): Diagnosis-only; zero source file changes; WP-001-whatsapp/docs/diagnosis-findings.md created; build PASS; root cause identified but not fully proven by runtime evidence.
QA Review (follops-qa): Verdict NEEDS-EVIDENCE — PASS for diagnosis work; FAIL to claim fully proven root cause (acceptance criteria 2, 3, 6 not met due to missing live verification); recommendation: perform live Evolution webhook health query + controlled inbound test + Vercel log capture + DB persistence check before any implementation.
Security Review (follops-security): Verdict PASS (no changes, no new secrets, no auth mutation, no RLS mutation, legacy service_role preserved as deferred P0). ESCALATE — pre-existing webhook signature/auth validation absent on public /api/whatsapp endpoint (P0 open per docs/context/SECURITY.md, INTEGRATIONS.md); recorded as finding; must NOT be silently redesigned outside approved scope.
Limitations: No live Evolution API access shown; no Vercel production logs shown; no DB query results shown; diagnosis relies on static/code inspection only.
Next Action (Blocked / needs live verification): (1) Execute live Evolution webhook health check (action=webhookInfo after reconnect) and save result; (2) Send controlled inbound WhatsApp message and capture Vercel POST /api/whatsapp log; (3) Confirm DB persistence (whatsapp_messages insert) and UI refresh; (4) Confirm syncEvolutionMessages recovery behavior. Once live evidence confirms Layer B/C cause, propose follow-up WP-002 for permanent self-healing (auto webhook verification after reconnect + periodic health poll + sync trigger after restart). No code fix deployed; no master merge.
Branch: WP-001-whatsapp (@ worktree WP-001-whatsapp/); master @ 981d03a (merged); branch pushed to origin; WP-001 status NOT marked Done until live verification completes and Security clears any future webhook/auth change.
